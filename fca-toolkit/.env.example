# Copy this file to .env.local and fill in real values. Never commit .env.local.

# Random secret used to sign download links so they can't be guessed or tampered with.
# Generate one locally with: openssl rand -base64 32
DOWNLOAD_SECRET=

# Added when we wire up Stripe Checkout (test-mode keys only until Matt says go live):
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
