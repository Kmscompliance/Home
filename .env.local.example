# Copy this file to .env.local and fill in real values.
# .env.local is gitignored — never commit real secrets.

# Anthropic API key, used by the quote engine and assistant (from Stage 1
# onward) to call Claude. Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
